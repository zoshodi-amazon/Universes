-- CoTypes/CoDependent/Default.lean
-- Coalgebraic dual of Types/Dependent/ — Cofibrations.
-- Re-export all CoDependent types.
-- One type per file (Invariant: 1-type-per-file).

import CoTypes.CoDependent.CoSchemaResult.Default
import CoTypes.CoDependent.CoNixSettings.Default
import CoTypes.CoDependent.CoSopsConfig.Default
import CoTypes.CoDependent.CoBootConfig.Default
import CoTypes.CoDependent.CoDisplayConfig.Default
import CoTypes.CoDependent.CoNetworkConfig.Default
import CoTypes.CoDependent.CoSshConfig.Default
import CoTypes.CoDependent.CoContainerConfig.Default
import CoTypes.CoDependent.CoSovereigntyConfig.Default
import CoTypes.CoDependent.CoGitConfig.Default
import CoTypes.CoDependent.CoBrowserConfig.Default
import CoTypes.CoDependent.CoAIConfig.Default
import CoTypes.CoDependent.CoCloudConfig.Default
import CoTypes.CoDependent.CoHomeTarget.Default
import CoTypes.CoDependent.CoMachineConfig.Default
import CoTypes.CoDependent.CoDiskConfig.Default
import CoTypes.CoDependent.CoPersistenceConfig.Default
import CoTypes.CoDependent.CoMachineUser.Default
import CoTypes.CoDependent.CoHardwareConfig.Default
