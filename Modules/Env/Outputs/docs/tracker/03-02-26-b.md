# 03-02-26-b — IOMainPhase Doc Sync

## Decisions

- All `IOPipelinePhase` references → `IOMainPhase` (fractal consistency at every level)
- All `IODeployPhase` references → `IOMainPhase` (deploy logic is inline in IOMainPhase)
- Stale recipe refs fixed: `just pipeline` → `just main`, `just types-export` → `just types-validate`
- `_backup/` left untouched as historical reference
- Historical tracker entries left untouched

## Changes

### Top-level docs
- `README.md` — all IOPipelinePhase/IODeployPhase refs → IOMainPhase, fixed OPTIONS recipes, fixed FILES tree, fixed CAVEATS
- `AGENTS.md` — all IOPipelinePhase refs → IOMainPhase (monadic identity, naming, invariants, anti-patterns, toolchain)

### Justfile + Nix comments
- `justfile` — header comment, user-identity recipe comment
- `IOUserPhase/Monads/IOPackagesPhase/default.nix` — IODeployPhase → IOMainPhase comment
- `IOUserPhase/Monads/IOIdentityPhase/default.nix` — IODeployPhase → IOMainPhase comment

### RL Lab
- Renamed `Monads/IOPipelinePhase/` → `Monads/IOMainPhase/`
- Updated `IOMainPhase/default.py` — docstring, class docstring, json_file path
- Updated `IOOptimizePhase/default.py` — docstring, import path
- Updated `justfile` — module path
- Updated `AGENTS.md` — phase table, compound phase descriptions
- Updated `README.md` — phase table, directory tree, description

### Fab Lab
- Renamed `Monads/IOPipelinePhase/` → `Monads/IOMainPhase/`
- Updated `justfile` — module path
- Updated `AGENTS.md` — phase table, compound phase description
- Updated `README.md` — phase table, directory tree

## Verification

- [ ] `grep -r IOPipelinePhase` returns only `_backup/` and historical tracker entries
- [ ] `grep -r IODeployPhase` returns only `_backup/` and historical tracker entries
- [ ] RL `just pipeline` resolves to `Monads.IOMainPhase.default`
- [ ] Fab `just pipeline` resolves to `Monads.IOMainPhase.default`
- [ ] `just main` runs top-level pipeline
